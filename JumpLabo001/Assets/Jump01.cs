using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Jump01 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //force = 4.0f;
    }

    // Update is called once per frame
    void Update()
    {
        //Transform.Position.y += force;
        //force -= 0.1f;
    }

    //float force;
}
